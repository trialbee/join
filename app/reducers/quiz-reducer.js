
export const initialState = {
    currentStep: null,
    steps: [],
    answers: []
}

export function quizReducer(state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}
